import { quotable } from '../lib'

describe('quotable.authors()', () => {
  describe('when called with no parameters', () => {
    test.todo('Response status is OK')
    test.todo('Response includes the expected pagination properties')
    test.todo('data.results is an array of Author objects')
  })
})
